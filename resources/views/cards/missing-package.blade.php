<div class="ls-notice" role="alert">
    <div class="ls-notice__title">spatie/laravel-backup is not installed</div>
    <p>
        The backup view depends on the
        <code>spatie/laravel-backup</code>
        package. Install it with
        <code>composer require spatie/laravel-backup</code>
        and publish its config via
        <code>php artisan vendor:publish --provider="Spatie\Backup\BackupServiceProvider" --tag=backup-config</code>.
    </p>
</div>
